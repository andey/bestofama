.row
  .col-12
    h1 Listing amas

.row
  .col-12
    table.table
      tr
        th
        th AMA
        th Comments
        th Responses
      - @amas.each do |ama|
        tr
          td
            - if ama.user.ops.count > 0
              span.glyphicon.glyphicon-user
          td
            strong= link_to truncate(ama.title, :length => 100), admin_ama_path(ama)
            br/
            span.muted
              = ama.date
              | &nbsp;by #{link_to ama.user.username, admin_user_path(ama.user)}&nbsp;
          td= ama.comments
          td= ama.responses
.row
  .col-12
    .center
      = will_paginate @amas