<div class="row">
  <div class=.col-8.col-sm-8.col-lg-8">
    <h1><%= @ama.key %>
      <small>Users</small>
    </h1>
  </div>
  <div class=.col-4.col-sm-4.col-lg-4" style="padding-top: 20px;">
    <%= link_to 'Add User', new_admin_ama_user_path, :class => 'btn pull-right' %>
  </div>
</div>

<table class="table">
  <tr>
    <th>Username</th>
    <th>Link</th>
    <th>Comment</th>
    <th>AMAs</th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.link_karma %></td>
        <td><%= user.comment_karma %></td>
        <td></td>
        <td><%= link_to 'Destroy', admin_ama_user_path(@ama.key, user), :method => :delete, :data => {:confirm => 'Are you sure?'} %></td>
      </tr>
  <% end %>
</table>
