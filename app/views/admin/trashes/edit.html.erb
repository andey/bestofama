<h1>Editing trash</h1>

<%= form_for(@trash, :url => admin_trash_path(@trash)) do |f| %>
    <% if @trash.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@trash.errors.count, "error") %> prohibited this trash from being saved:</h2>

          <ul>
            <% @trash.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :key %>
      <br/>
      <%= f.text_field :key %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>


<%= link_to 'Show', admin_trash_path(@trash) %> |
<%= link_to 'Back', admin_trashes_path %>
